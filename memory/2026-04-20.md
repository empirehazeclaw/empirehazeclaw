
## 🔑 Secrets Cleanup + Key Audit Learnings (15:19 UTC)

### Was passiert ist:
1. **Full Key Audit** durchgeführt — 44 Keys in secrets.env
2. **32 Keys als defekt identifiziert** → entfernt
3. **12 funktionierende Keys** blieben
4. **Secrets konsolidiert** — eine canonical `secrets.env` (nicht 4 Kopien)
5. **Brave → Tavily** in research_agent.py umgestellt (Key defekt)
6. **Brave hardcoded Key** aus openclaw.json entfernt

### Wichtige Learnings:
- **MINIMAX_API_KEY** nutzt `https://api.minimax.io/anthropic/v1/messages` (NICHT `/v1/text/chatcompletion_v2`)
- **HF Token role:read** = nur Read-only, keine Inference API
- **OpenClaw liest Secrets aus `openclaw.json`** hardcoded (nicht secrets.env) für Plugin-Keys
- **Brave API Key ist kommerziell** — 403 = Key ungültig/deaktiviert
- **Tavily** = funktionierender Free-Search-Alternative

### Secrets Locations (Kanonisch):
- `/home/clawbot/.openclaw/secrets.env` — ✅ AKTIV (12 Keys)
- Backups: `/home/clawbot/.openclaw/backups/secrets_*.env`

### Active Scripts Secrets Path:
- Alle Scripts nutzen `Path("/home/clawbot/.openclaw/secrets.env")` ✅

### Nie wieder:
- Keine Keys hardcoded in openclaw.json (außer Plugin-Bundled)
- Keine 4 Kopien von secrets.env
- Brave nicht mehr verwenden
