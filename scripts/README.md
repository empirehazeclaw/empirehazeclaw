# Sir HazeClaw Scripts

_Generated: 2026-04-10 22:50 UTC_

**Total Scripts:** 56
**With Tests:** 0

---

## 📁 Scripts by Category

### Auto

_3 scripts_

- ⚠️ `auto_backup.py` — Sir HazeClaw Auto Backup — IMPROVED
Automatischer Workspace 
- ⚠️ `auto_doc.py` — Sir HazeClaw Auto Documentation Generator
Generiert automati
- ⚠️ `auto_session_capture.py` — Auto Session Capture — Zettelkasten Daily Reminder v2
Erfüll

### Automated

_1 scripts_

- ⚠️ `automated_outreach.py` — Automated Outreach via Gmail SMTP
Sends emails using Google 

### Backup

_1 scripts_

- ⚠️ `backup_verify.py` — Sir HazeClaw Backup Verification Script
Prüft ob Backups exi

### Batch

_1 scripts_

- ⚠️ `batch_exec.py` — BATCH EXEC OPTIMIZER
Combines multiple shell commands into o

### Common

_1 scripts_

- ⚠️ `common_issues_check.py` — Sir HazeClaw Common Issues Check
Prüft auf häufige Probleme.

### Crm

_1 scripts_

- ⚠️ `crm_manager.py` — 🦞 CRM Manager - Lead Status & Pipeline Tracking

### Cron

_2 scripts_

- ⚠️ `cron_monitor.py` — Sir HazeClaw Cron Monitor — IMPROVED
Prüft Status der Cron J
- ⚠️ `cron_watchdog.py` — Sir HazeClaw Cron Watchdog
Überwacht Cron Jobs und meldet we

### Daily

_1 scripts_

- ⚠️ `daily_summary.py` — Sir HazeClaw Daily Summary Generator — IMPROVED
Erstellt ein

### Deep

_1 scripts_

- ⚠️ `deep_reflection.py` — Sir HazeClaw Deep Self-Reflection
Tiefgehende Selbst-Analyse

### Demo

_1 scripts_

- ⚠️ `demo_scheduler.py` — 📅 Demo Scheduler - Skill
Wenn ein Lead auf unsere Outreach E

### Deploy

_1 scripts_

- ⚠️ `deploy_safety.py` — 🔒 Deployment Safety Checker
Run before every deploy to ensur

### Discord

_1 scripts_

- ⚠️ `discord_report_forwarder.py` — Discord Report Forwarder
Posts agent reports to Discord chan

### Email

_1 scripts_

- ⚠️ `email_sequence.py` — 🦞 Email Sequence Manager - Automated Follow-ups

### Evening

_2 scripts_

- ⚠️ `evening_summary.py` — Sir HazeClaw Evening Summary — IMPROVED
Generiert einen Aben
- ⚠️ `evening_capture.py` — Evening Capture - Zettelkasten Daily Reminder
Creates a new 

### Health

_3 scripts_

- ⚠️ `health_alert.py` — Sir HazeClaw Health Alert Script
Sendet Alerts wenn System P
- ⚠️ `health_dashboard.py` — 🏥 Health Dashboard — EmpireHazeClaw System Health Check

Che
- ⚠️ `health_monitor.py` — Sir HazeClaw Health Monitor — IMPROVED
Prüft System-Gesundhe

### Idempotency

_1 scripts_

- ⚠️ `idempotency_check.py` — Sir HazeClaw Idempotency Checker
Prüft ob Scripts idempotent

### Improved

_1 scripts_

- ⚠️ `improved_outreach.py` — 📧 Improved Outreach Generator - Skill
Erstellt personalisier

### Kg

_1 scripts_

- ⚠️ `kg_updater.py` — Sir HazeClaw Knowledge Graph Updater
Fügt neue Entries zum K

### Kgml

_1 scripts_

- ⚠️ `kgml_summary.py` — KGML Summary Generator
Wandelt Knowledge Graph in lesbare Ma

### Lcm

_1 scripts_

- ⚠️ `lcm_wiki_sync.py` — 📚 LCM Wiki-Sync — Konvertiert LCM-Summaries zu Wiki Permanen

### Lead

_1 scripts_

- ⚠️ `lead_generator.py` — Lead Generator for EmpireHazeClaw
Finds German businesses (K

### Llm

_1 scripts_

- ⚠️ `llm_outreach.py` — 📧 LLM-Powered Outreach - Skill
Erstellt wirklich personalisi

### Meeting

_1 scripts_

- ⚠️ `meeting_scheduler.py` — 🦞 Meeting Scheduler - Books demo meetings automatically
When

### Memory

_2 scripts_

- ⚠️ `memory_cleanup.py` — Memory Cleanup System — IMPROVED
Tägliche Speicherpflege mit
- ⚠️ `memory_hybrid_search.py` — Sir HazeClaw Hybrid Memory Search — IMPROVED
Combines Keywor

### Model

_1 scripts_

- ⚠️ `model_config.py` — Unified Model Config for EmpireHazeClaw
Central place for al

### Morning

_1 scripts_

- ⚠️ `morning_brief.py` — Sir HazeClaw Morning Brief — IMPROVED
Generiert einen Mornin

### Openrouter

_1 scripts_

- ⚠️ `openrouter_monitor.py` — Sir HazeClaw OpenRouter Monitor
Überwacht ob OpenRouter API 

### Other

_2 scripts_

- ⚠️ `vault.py` — Sir HazeClaw — Encrypted Vault für Secrets
Verschlüsselung: 
- ⚠️ `evolve.py` — 🦞 Capability Evolver - Improves Agent Capabilities Over Time

### Outreach

_1 scripts_

- ⚠️ `outreach_optimizer.py` — 🦞 Outreach Optimizer - Analyzes email performance and sugges

### Priority

_1 scripts_

- ⚠️ `priority_filter.py` — AI Priority Filter - Find the Most Important Items
Scoring b

### Quality

_1 scripts_

- ⚠️ `quality_metrics.py` — Sir HazeClaw Quality Metrics Tracker
Misst Qualität der Arbe

### Quick

_2 scripts_

- ⚠️ `quick_check.py` — Sir HazeClaw Quick Check
Schneller System-Check für tägliche
- ⚠️ `quick_outreach.py` — Quick Outreach - Sendet personalisierte E-Mails an Leads.
Us

### Reschedule

_1 scripts_

- ⚠️ `reschedule_sovereign.py` — Phoenix Automation — Self-Rescheduling for Sovereign Agents


### Response

_1 scripts_

- ⚠️ `response_tracker.py` — 📊 Response Tracker - Skill
Trackt Antworten auf unsere Outre

### Revenue

_1 scripts_

- ⚠️ `revenue_forecaster.py` — 💰 Revenue Forecaster - Skill
Prognostiziert Revenue basieren

### Self

_1 scripts_

- ⚠️ `self_check.py` — Sir HazeClaw Self-Check — IMPROVED
Überprüft System Health, 

### Semantic

_1 scripts_

- ⚠️ `semantic_search.py` — Semantic Search - Local Embeddings with sentence-transformer

### Session

_2 scripts_

- ⚠️ `session_cleanup.py` — Session Cleanup - Delete sessions older than 7 days.
- ⚠️ `session_memory_manager.py` — 💾 Session Memory Manager - Auto-saves important context
Inte

### System

_1 scripts_

- ⚠️ `system_report.py` — Sir HazeClaw Master System Report
Generiert einen umfassende

### Telegram

_3 scripts_

- ⚠️ `telegram_alert.py` — 📱 Telegram Alert Script
Sendet Alerts nur bei PROBLEMEN/WARN
- ⚠️ `telegram_memory_extractor.py` — 📥 Telegram Chat History Extractor & Memory Integrator v2
===
- ⚠️ `telegram_parser.py` — Telegram HTML Export Parser
Extrahiert Messages aus Telegram

### Vercel

_1 scripts_

- ⚠️ `vercel_monitor.py` — 🚀 Vercel Autopilot
Continuous monitoring and improvement for

### Verify

_1 scripts_

- ⚠️ `verify_delivery.py` — LINK VERIFIER - Pruft alle URLs vor dem Senden

Usage:
    p

### Weekly

_2 scripts_

- ⚠️ `weekly_review.py` — Sir HazeClaw Weekly Review — IMPROVED
Generiert einen wöchen
- ⚠️ `weekly_review_zettel.py` — Weekly Zettelkasten Review
- Erstellt wöchentliche Zusammenf

---

## 📊 All Scripts

### ⚠️ `auto_backup.py`

_Sir HazeClaw Auto Backup — IMPROVED
Automatischer Workspace Backup mit Verifizierung.

Features:
- Backup Erstellung mit Kompression
- Git Commit (wenn Änderungen)
- Alte Backups aufräumen (Retention)
- Backup Verifizierung
- Detailliertes Reporting

Usage:
    python3 auto_backup.py
    python3 auto_backup.py --dry-run
    python3 auto_backup.py --verify_

**Functions:**
- `get_backup_stats()` — Holt Backup Statistiken.
- `create_backup()` — Erstellt Workspace Backup.
- `verify_backup(backup_path)` — Verifiziert Backup Integrity.
- `get_git_status()` — Prüft Git Status.
- `git_backup()` — Macht Git Commit wenn Änderungen.

| Modified | Size |
|----------|------|
| 2026-04-10 | 7.3KB |

### ⚠️ `auto_doc.py`

_Sir HazeClaw Auto Documentation Generator
Generiert automatisch Dokumentation aus Script-Metadaten.

Usage:
    python3 auto_doc.py
    python3 auto_doc.py --scripts-dir scripts
    python3 auto_doc.py --update-readme_

**Functions:**
- `extract_docstring(filepath)` — Extrahiert Docstring aus Python Script.
- `extract_functions(filepath)` — Extrahiert Functions und ihre Docs aus Script.
- `get_script_info(filepath)` — Sammelt alle Infos über ein Script.
- `generate_script_entry(info)` — Generiert Markdown-Eintrag für ein Script.
- `generate_readme(scripts)` — Generiert komplette README.md.

| Modified | Size |
|----------|------|
| 2026-04-10 | 7.4KB |

### ⚠️ `automated_outreach.py`

_Automated Outreach via Gmail SMTP
Sends emails using Google App Password_

**Functions:**
- `load_json(path, default)`
- `save_json(path, data)`
- `send_email(to_email, subject, body)` — Send email via Gmail SMTP
- `get_email_content(email, step, name)` — Get email template
- `get_pending_leads()` — Get leads ready to email

| Modified | Size |
|----------|------|
| 2026-04-10 | 5.9KB |

### ⚠️ `backup_verify.py`

_Sir HazeClaw Backup Verification Script
Prüft ob Backups existieren und validiert sie.

Usage:
    python3 backup_verify.py
    python3 backup_verify.py --fix  # Erstellt backups wenn fehlen_

**Functions:**
- `check_server_backup()` — Prüft ob Server Backup existiert.
- `check_github_backup()` — Prüft ob GitHub Commit heute existiert.
- `check_backup_size()` — Prüft ob Backup sinnvolle Größe hat.
- `verify_backup_integrity()` — Verifiziert Backup Integrity.
- `cleanup_old_backups()` — Entfernt alte Backups.

| Modified | Size |
|----------|------|
| 2026-04-10 | 6.0KB |

### ⚠️ `common_issues_check.py`

_Sir HazeClaw Common Issues Check
Prüft auf häufige Probleme.

Usage:
    python3 common_issues_check.py_

**Functions:**
- `check_issues()` — Prüft auf häufige Issues.
- `main()`

| Modified | Size |
|----------|------|
| 2026-04-10 | 3.3KB |

### ⚠️ `crm_manager.py`

_🦞 CRM Manager - Lead Status & Pipeline Tracking_

**Functions:**
- `load_leads()` — Load leads from CSV
- `load_status()` — Load lead status tracking
- `save_status(status)` — Save lead status tracking
- `load_responses()` — Load customer responses
- `save_responses(responses)` — Save customer responses

| Modified | Size |
|----------|------|
| 2026-04-10 | 6.4KB |

### ⚠️ `cron_monitor.py`

_Sir HazeClaw Cron Monitor — IMPROVED
Prüft Status der Cron Jobs mit detaillierten Informationen.

Usage:
    python3 cron_monitor.py
    python3 cron_monitor.py --format telegram
    python3 cron_monitor.py --failed-only_

**Functions:**
- `get_cron_status()` — Liest Cron Jobs aus jobs.json.
- `get_last_run_time(state)` — Extrahiert Letzte Laufzeit.
- `format_job_info(job, verbose)` — Formatiert Job Information.
- `generate_report(format, failed_only)` — Generiert Cron Report.
- `main()`

| Modified | Size |
|----------|------|
| 2026-04-10 | 5.7KB |

### ⚠️ `cron_watchdog.py`

_Sir HazeClaw Cron Watchdog
Überwacht Cron Jobs und meldet wenn welche fehlschlagen.

Usage:
    python3 cron_watchdog.py_

**Functions:**
- `load_jobs()`
- `check_jobs(jobs)` — Prüft Jobs auf Probleme.
- `main()`

| Modified | Size |
|----------|------|
| 2026-04-10 | 2.4KB |

### ⚠️ `daily_summary.py`

_Sir HazeClaw Daily Summary Generator — IMPROVED
Erstellt eine tägliche Zusammenfassung der Aktivitäten.

Usage:
    python3 daily_summary.py
    python3 daily_summary.py --date 2026-04-10
    python3 daily_summary.py --format telegram_

**Functions:**
- `get_git_commits(days_ago)` — Holt Git Commits für bestimmten Tag.
- `get_memory_notes_today()` — Holt Memory Notes von heute.
- `get_kg_stats()` — Holt KG Stats.
- `get_system_health()` — Holt System Health.
- `get_cron_summary()` — Holt Cron Zusammenfassung.

| Modified | Size |
|----------|------|
| 2026-04-10 | 9.7KB |

### ⚠️ `deep_reflection.py`

_Sir HazeClaw Deep Self-Reflection
Tiefgehende Selbst-Analyse für kontinuierliche Verbesserung.

Usage:
    python3 deep_reflection.py
    python3 deep_reflection.py --days 7
    python3 deep_reflection.py --output kg_

**Functions:**
- `get_git_activity(days)` — Holt Git Activity für letzte X Tage.
- `get_todays_memory()` — Holt今天的 Memory Notes.
- `get_patterns()` — Liest bekannte Patterns.
- `analyze_quality(commits, memory_content)` — Analysiert Quality der Arbeit.
- `generate_deep_reflection()` — Generiert tiefgehende Selbst-Reflection.

| Modified | Size |
|----------|------|
| 2026-04-10 | 9.1KB |

### ⚠️ `demo_scheduler.py`

_📅 Demo Scheduler - Skill
Wenn ein Lead auf unsere Outreach Email antwortet, automatisch einen Demo-Call vorschlagen

Nutzung: python3 scripts/demo_scheduler.py --email "lead@example.com" --name "Max Mustermann"_

**Functions:**
- `load_demos()`
- `save_demos(demos)`
- `create_demo_request(lead_email, lead_name, company)` — Erstelle Demo-Anfrage
- `send_confirmation(lead_email, lead_name, company)` — Sende Bestätigung mit Demo-Vorschlag
- `main()`

| Modified | Size |
|----------|------|
| 2026-04-10 | 4.1KB |

### ⚠️ `discord_report_forwarder.py`

_Discord Report Forwarder
Posts agent reports to Discord channels/threads
Run after cron jobs complete (e.g., via cron or manually)_

**Functions:**
- `send_discord(channel_id, message)` — Send message to Discord channel/thread
- `format_status(emoji, text)` — Format status with emoji
- `load_report(path)` — Load a JSON report file
- `generate_report_message(agent, report)` — Generate a Discord-formatted report message
- `main()` — Main function - post all reports to Discord

| Modified | Size |
|----------|------|
| 2026-04-10 | 5.4KB |

### ⚠️ `email_sequence.py`

_🦞 Email Sequence Manager - Automated Follow-ups_

**Functions:**
- `load_sent()` — Load sent emails tracking
- `save_sent(sent)` — Save sent emails tracking
- `load_sequences()` — Load sequence definitions
- `save_sequences(sequences)` — Save sequence definitions
- `init_sequences()` — Initialize sequences for all leads

| Modified | Size |
|----------|------|
| 2026-04-10 | 8.2KB |

### ⚠️ `evening_summary.py`

_Sir HazeClaw Evening Summary — IMPROVED
Generiert einen Abend-Zusammenfassung mit echten Highlights.

Usage:
    python3 evening_summary.py
    python3 evening_summary.py --format telegram_

**Functions:**
- `get_today_commits()` — Holt Git Commits von heute.
- `get_yesterday_commits()` — Holt Git Commits von gestern.
- `get_today_memory()` — Holt Memory Notes von heute.
- `get_kg_today()` — Holt KG Stats und Änderungen heute.
- `get_system_status()` — Holt aktuellen System Status.

| Modified | Size |
|----------|------|
| 2026-04-10 | 8.3KB |

### ⚠️ `health_alert.py`

_Sir HazeClaw Health Alert Script
Sendet Alerts wenn System Probleme hat.

Usage:
    python3 health_alert.py
    python3 health_alert.py --test  # Test alert only_

**Functions:**
- `log_alert(alert_type, message)` — Loggt Alert zu Datei.
- `should_alert(alert_type)` — Prüft ob Alert gesendet werden soll (cooldown).
- `check_gateway()` — Prüft Gateway.
- `check_disk()` — Prüft Disk.
- `check_memory()` — Prüft Memory.

| Modified | Size |
|----------|------|
| 2026-04-10 | 4.3KB |

### ⚠️ `health_dashboard.py`

_🏥 Health Dashboard — EmpireHazeClaw System Health Check

Checkt:
- Gateway Status (RPC, Prozess)
- Cron Jobs Status
- Disk/Memory Usage
- OpenClaw Prozesse

Output: Kompakter Status-Report für Builder/CEO_

**Functions:**
- `check_gateway()` — Check Gateway Status.
- `check_disk()` — Check Disk Usage.
- `check_memory()` — Check Memory Usage.
- `check_crons()` — Check Cron Jobs Status.
- `check_processes()` — Check wichtige Prozesse.

| Modified | Size |
|----------|------|
| 2026-04-10 | 7.7KB |

### ⚠️ `health_monitor.py`

_Sir HazeClaw Health Monitor — IMPROVED
Prüft System-Gesundheit und alarmiert bei Problemen.

Usage:
    python3 health_monitor.py [--report]
    python3 health_monitor.py [--check-gateway] [--check-disk] [--check-crons]_

**Functions:**
- `check_gateway()` — Prüft ob Gateway läuft.
- `check_disk()` — Prüft Disk Space.
- `check_memory()` — Prüft Memory Usage.
- `check_load()` — Prüft System Load.
- `check_kg()` — Prüft Knowledge Graph Health.

| Modified | Size |
|----------|------|
| 2026-04-10 | 11.2KB |

### ⚠️ `idempotency_check.py`

_Sir HazeClaw Idempotency Checker
Prüft ob Scripts idempotent sind (mehrfach ausführbar ohne Probleme).

Usage:
    python3 idempotency_check.py
    python3 idempotency_check.py --fix_

**Functions:**
- `check_script(script_path)` — Prüft ein Script auf Idempotenz.
- `main()`

| Modified | Size |
|----------|------|
| 2026-04-10 | 1.7KB |

### ⚠️ `improved_outreach.py`

_📧 Improved Outreach Generator - Skill
Erstellt personalisierte Outreach Emails basierend auf Lead-Daten

Nutzung: python3 scripts/improved_outreach.py --lead "Restaurant Beispiel" --industry gastro --email test@firma.de_

**Functions:**
- `get_industry_key(industry)` — Erkenne Industry Key
- `personalize_email(company, industry)` — Erstelle personalisierte Email
- `send_email(to_email, subject, body)` — Sende Email
- `main()`

| Modified | Size |
|----------|------|
| 2026-04-10 | 5.0KB |

### ⚠️ `kg_updater.py`

_Sir HazeClaw Knowledge Graph Updater
Fügt neue Entries zum Knowledge Graph hinzu.

⚠️ WICHTIG: Dieser Script verwendet das echte KG Format (entities/relations)
   NICHT das alte nodes/edges Format!

Usage:
    python3 kg_updater.py add --type skill --name "New Skill" --content "Description"
    python3 kg_updater.py list
    python3 kg_updater.py stats
    python3 kg_updater.py search "query"
    python3 kg_updater.py add-relation --from "EntityA" --to "EntityB" --type "relates_to"_

**Functions:**
- `load_kg()` — Lädt den Knowledge Graph.
- `save_kg(kg)` — Speichert den Knowledge Graph.
- `init_kg()` — Initialisiert KG falls nicht vorhanden.
- `add_entity(entity_type, name, content)` — Fügt Entity zum KG hinzu.
- `list_entities()` — Listet alle Entities nach Typ.

| Modified | Size |
|----------|------|
| 2026-04-10 | 8.5KB |

### ⚠️ `kgml_summary.py`

_KGML Summary Generator
Wandelt Knowledge Graph in lesbare Markdown Summary um.

Verwendung:
    python3 kgml_summary.py [kg_json] [output_md]_

**Functions:**
- `generate_kgml(kg_path, output_path)` — Generiert KGML Summary aus KG JSON.
- `main()`

| Modified | Size |
|----------|------|
| 2026-04-10 | 3.2KB |

### ⚠️ `lcm_wiki_sync.py`

_📚 LCM Wiki-Sync — Konvertiert LCM-Summaries zu Wiki Permanent Notes

Liest von: wiki_sync_queue.json
Schreibt nach: memory/notes/permanent/

Hybrid-Lösung Teil 2:
- evening_capture.py → fleeting notes + sync queue
- lcm_wiki_sync.py → permanent wiki notes aus queue

Pattern: Karpathy LLM Wiki — persistente, kumulierende Wissensbasis_

**Functions:**
- `categorize(topics, learnings)` — Kategorisiert basierend auf Topics und Learnings.
- `extract_summary(content, max_len)` — Extrahiert erste aussagekräftige Sätze.
- `format_learnings(learnings)` — Formatiert Learnings als Markdown Liste.
- `format_next_actions(learnings, topics)` — Leitet mögliche nächste Actions aus Learnings/Topi
- `find_related_notes(topics, existing_notes)` — Findet verwandte Notes basierend auf Topics.

| Modified | Size |
|----------|------|
| 2026-04-10 | 12.2KB |

### ⚠️ `lead_generator.py`

_Lead Generator for EmpireHazeClaw
Finds German businesses (KMUs) that could benefit from AI agents_

**Functions:**
- `search_google(query, limit)` — Search using Tavily API (we have this key)
- `extract_emails_from_website(url)` — Try to find contact email on website
- `search_business_type(business_type, location, limit)` — Search for businesses of a specific type
- `add_to_csv(leads, csv_file)` — Add leads to CSV
- `main()`

| Modified | Size |
|----------|------|
| 2026-04-10 | 4.7KB |

### ⚠️ `llm_outreach.py`

_📧 LLM-Powered Outreach - Skill
Erstellt wirklich personalisierte Emails mit echter KI

Nutzung: python3 scripts/llm_outreach.py --lead "Max Mustermann" --company "Restaurant Goldener Löwen" --industry gastro --email max@firma.de_

**Functions:**
- `template_fallback(lead_name, company, industry)` — Fallback wenn LLM nicht verfügbar
- `get_industry_key(industry)`
- `generate_personalized_email(lead_name, company, industry)` — Generiere personalisierte Email mit LLM
- `send_email(to_email, subject, body)` — Sende Email
- `main()`

| Modified | Size |
|----------|------|
| 2026-04-10 | 10.8KB |

### ⚠️ `meeting_scheduler.py`

_🦞 Meeting Scheduler - Books demo meetings automatically
When a lead responds, schedules a calendar slot_

**Functions:**
- `load_meetings(self)` — Load existing meetings
- `save_meetings(self)` — Save meetings to file
- `get_next_slot(self, preferred_day)` — Get the next available slot
- `schedule_meeting(self, lead_email, lead_name)` — Schedule a meeting with a lead
- `generate_meet_link(self)` — Generate a Google Meet or video link placeholder

| Modified | Size |
|----------|------|
| 2026-04-10 | 7.3KB |

### ⚠️ `memory_cleanup.py`

_Memory Cleanup System — IMPROVED
Tägliche Speicherpflege mit Safety Checks.

Features:
- Fleeting Notes archivieren (7+ Tage)
- Weekly consolidation
- KG Pruning (Orphan Detection)
- Log Rotation (30+ Tage)
- Dry-run mode
- Backup vor Löschen
- Detailliertes Reporting

Usage:
    python3 memory_cleanup.py
    python3 memory_cleanup.py --dry-run
    python3 memory_cleanup.py --report_

**Functions:**
- `log(msg, emoji)`
- `get_file_age_days(path)` — Gibt das Alter einer Datei in Tagen zurück.
- `get_file_age_hours(path)` — Gibt das Alter einer Datei in Stunden zurück.
- `create_backup()` — Erstellt Backup vor dem Cleanup.
- `cleanup_fleeting_notes()` — Archiviert oder löscht alte fleeting notes.

| Modified | Size |
|----------|------|
| 2026-04-10 | 12.5KB |

### ⚠️ `memory_hybrid_search.py`

_Sir HazeClaw Hybrid Memory Search — IMPROVED
Combines Keyword + Semantic + Context-Aware Search.

Improvements:
- Context awareness (current situation)
- Date-based relevance weighting
- Better snippet extraction
- KG entity search improved
- Recent memories prioritized

Usage:
    python3 memory_hybrid_search.py <query>
    python3 memory_hybrid_search.py <query> --limit 10
    python3 memory_hybrid_search.py <query> --days 7_

**Functions:**
- `get_file_age_days(filepath)` — Gibt Alter einer Datei in Tagen zurück.
- `calculate_recency_score(filepath)` — Berechnet Recency Score (jünger = höherer Score).
- `keyword_search(query, files)` — Sucht nach Keywords in Dateien.
- `calculate_keyword_score(query_words, filepath)` — Berechnet Keyword Match Score.
- `semantic_search(query, limit)` — Sucht semantisch im Knowledge Graph.

| Modified | Size |
|----------|------|
| 2026-04-10 | 9.3KB |

### ⚠️ `morning_brief.py`

_Sir HazeClaw Morning Brief — IMPROVED
Generiert einen Morning Brief für Master mit höherem Value.

Improvements:
- KG stats mit Growth
- Active blockers
- Yesterday comparison
- Actionable recommendations
- Pattern alerts

Usage:
    python3 morning_brief.py
    python3 morning_brief.py --format telegram_

**Functions:**
- `get_system_status()` — Holt System Status.
- `get_kg_stats()` — Holt KG Stats mit Growth.
- `get_active_blockers()` — Liest Active Blockers aus HEARTBEAT.md.
- `get_cron_status()` — Holt Cron Status.
- `get_backup_status()` — Holt Backup Status.

| Modified | Size |
|----------|------|
| 2026-04-10 | 10.1KB |

### ⚠️ `openrouter_monitor.py`

_Sir HazeClaw OpenRouter Monitor
Überwacht ob OpenRouter API funktioniert.

Usage:
    python3 openrouter_monitor.py_

**Functions:**
- `get_openrouter_config()` — Liest OpenRouter Config.
- `check_openrouter()` — Testet OpenRouter API.
- `log_status(ok, msg)` — Loggt Status.
- `main()`

| Modified | Size |
|----------|------|
| 2026-04-10 | 2.5KB |

### ⚠️ `outreach_optimizer.py`

_🦞 Outreach Optimizer - Analyzes email performance and suggests improvements
Tracks which templates/approaches get responses_

**Functions:**
- `load_data(self)` — Load all outreach data
- `analyze_response_rate(self, data)` — Calculate response rates by various factors
- `analyze_by_industry(self, data)` — Analyze which industries respond best
- `suggest_improvements(self, data)` — Generate improvement suggestions based on data
- `generate_report(self)` — Generate full optimization report

| Modified | Size |
|----------|------|
| 2026-04-10 | 8.0KB |

### ⚠️ `priority_filter.py`

_AI Priority Filter - Find the Most Important Items
Scoring based on: Business Impact, System Stability, Nico's Goals_

**Functions:**
- `score_item(text, category)` — Score an item based on keyword analysis
- `extract_key_info(text)` — Extract key information from text
- `main()`

| Modified | Size |
|----------|------|
| 2026-04-10 | 5.8KB |

### ⚠️ `quality_metrics.py`

_Sir HazeClaw Quality Metrics Tracker
Misst Qualität der Arbeit und trackt Verbesserungen.

Metriken:
- Tasks completed vs planned
- Scripts tested vs untested
- Commits per day
- Backup ratio (Backups/Commits)
- Error rate
- Pattern violations

Usage:
    python3 quality_metrics.py
    python3 quality_metrics.py --days 7
    python3 quality_metrics.py --report_

**Functions:**
- `get_commits_per_day(days)` — Holt Commits pro Tag für letzte X Tage.
- `get_backups_per_day(days)` — Holt Backups pro Tag für letzte X Tage.
- `calculate_metrics(days)` — Berechnet alle Quality Metriken.
- `generate_report(metrics)` — Generiert Quality Report.
- `save_metrics_to_kg(metrics)` — Speichert Metrics in KG.

| Modified | Size |
|----------|------|
| 2026-04-10 | 8.5KB |

### ⚠️ `quick_check.py`

_Sir HazeClaw Quick Check
Schneller System-Check für tägliche Verwendung.

Usage:
    python3 quick_check.py_

**Functions:**
- `check(name, ok, msg)`
- `main()`

| Modified | Size |
|----------|------|
| 2026-04-10 | 2.3KB |

### ⚠️ `quick_outreach.py`

_Quick Outreach - Sendet personalisierte E-Mails an Leads.
Usage: python3 quick_outreach.py --limit 10_

**Functions:**
- `load_sent()` — Lade bereits gesendete IDs
- `save_sent(sent_ids)` — Speichere gesendete IDs
- `get_industry(email)` — Rate industry basierend auf Email/Domain
- `humanize_name(company)` — Extrahiere einen Vornamen aus Firmenname
- `send_email(to_email, subject, body)` — Sende E-Mail via GOG CLI

| Modified | Size |
|----------|------|
| 2026-04-10 | 4.1KB |

### ⚠️ `response_tracker.py`

_📊 Response Tracker - Skill
Trackt Antworten auf unsere Outreach Emails

Nutzung: python3 scripts/response_tracker.py_

**Functions:**
- `decode_str(s)` — Decode email header string
- `get_sent_emails()` — Lade Liste der gesendeten Emails
- `check_inbox_for_responses()` — Prüfe ob wir Antworten auf unsere Outreach Emails 
- `save_responses(responses)` — Speichere gefundene Responses
- `main()`

| Modified | Size |
|----------|------|
| 2026-04-10 | 4.7KB |

### ⚠️ `revenue_forecaster.py`

_💰 Revenue Forecaster - Skill
Prognostiziert Revenue basierend auf 30-Tage-Daten

Integration: Fügt sich in daily_report ein
Nutzung: python3 scripts/revenue_forecaster.py_

**Functions:**
- `get_stripe_balance()` — Hole Stripe Balance (tatsächliches Guthaben)
- `get_stripe_charges(days)` — Hole Stripe Charges der letzten X Tage
- `get_stripe_subscriptions()` — Hole aktive Subscriptions
- `calculate_daily_revenue(charges, days)` — Berechne täglichen Revenue
- `calculate_mrr(subscriptions)` — Berechne Monthly Recurring Revenue

| Modified | Size |
|----------|------|
| 2026-04-10 | 6.6KB |

### ⚠️ `self_check.py`

_Sir HazeClaw Self-Check — IMPROVED
Überprüft System Health, Patterns und Workflow.

Usage:
    python3 self_check.py_

**Functions:**
- `check_gateway()` — Check if gateway is responding.
- `check_disk()` — Check disk space.
- `check_memory()` — Check memory usage.
- `check_load()` — Check system load.
- `check_kg()` — Check Knowledge Graph health.

| Modified | Size |
|----------|------|
| 2026-04-10 | 8.3KB |

### ⚠️ `system_report.py`

_Sir HazeClaw Master System Report
Generiert einen umfassenden System-Report.

Usage:
    python3 system_report.py
    python3 system_report.py --format markdown_

**Functions:**
- `check_gateway()`
- `check_disk()`
- `check_memory()`
- `check_load()`
- `check_databases()`

| Modified | Size |
|----------|------|
| 2026-04-10 | 5.5KB |

### ⚠️ `vault.py`

_Sir HazeClaw — Encrypted Vault für Secrets
Verschlüsselung: AES-256-GCM mit PBKDF2

Verwendung:
    python3 vault.py set <name> <value> [-n note]
    python3 vault.py get <name>
    python3 vault.py list_

**Functions:**
- `get_or_create_key()` — Hole existierenden Key oder erstelle neuen.
- `derive_key(passphrase, salt)` — PBKDF2 Key Derivation.
- `encrypt(plaintext, key_hex)` — Verschlüsselt plaintext mit AES-256-GCM.
- `decrypt(data, key_hex)` — Entschlüsselt AES-256-GCM data.
- `vault_set(name, value, note)` — Speichert einen Secret.

| Modified | Size |
|----------|------|
| 2026-04-10 | 5.0KB |

### ⚠️ `weekly_review.py`

_Sir HazeClaw Weekly Review — IMPROVED
Generiert einen wöchentlichen Review mit echten Insights.

Improvements:
- Week-over-week comparison
- KG growth tracking
- Trending metrics
- Actionable insights
- Dynamic goals from patterns

Usage:
    python3 weekly_review.py
    python3 weekly_review.py --format telegram_

**Functions:**
- `get_week_commits(week_ago)` — Holt Git Commits für bestimmte Woche.
- `get_daily_commits()` — Holt tägliche Commits für die Woche.
- `get_kg_stats()` — Holt KG Stats.
- `get_script_stats()` — Zählt Scripts.
- `get_cron_stats()` — Holt Cron Stats.

| Modified | Size |
|----------|------|
| 2026-04-10 | 10.4KB |

### ⚠️ `auto_session_capture.py`

_Auto Session Capture — Zettelkasten Daily Reminder v2
Erfüllt: Erstellt eine fleeting Note MIT Inhalt (nicht nur ein Template)

分析: Liest Session-Transcripts und extrahiert Key-Takeaways_

**Functions:**
- `find_recent_sessions()` — Findet Session-Dateien der letzten 24 Stunden.
- `extract_content_from_session(session_file)` — Extrahiert Text-Content aus einer Session-JSONL Da
- `analyze_content(content)` — Analysiert Content und extrahiert Learnings, Tasks
- `create_summary(content, learnings, files)` — Erstellt eine kurze Zusammenfassung.
- `create_daily_note()` — Hauptfunktion: Erstellt die tägliche Auto-Capture 

| Modified | Size |
|----------|------|
| 2026-04-09 | 7.3KB |

### ⚠️ `batch_exec.py`

_BATCH EXEC OPTIMIZER
Combines multiple shell commands into one call to reduce exec overhead

Usage:
  python3 batch_exec.py --commands "cmd1 && cmd2" "cmd3 && cmd4"_

**Functions:**
- `batch_execute(commands)` — Execute multiple commands in sequence, return comb
- `main()`

| Modified | Size |
|----------|------|
| 2026-04-09 | 1.5KB |

### ⚠️ `evening_capture.py`

_Evening Capture - Zettelkasten Daily Reminder
Creates a new fleeting note template for today_

**Functions:**
- `create_daily_note()`

| Modified | Size |
|----------|------|
| 2026-04-09 | 1.2KB |

### ⚠️ `reschedule_sovereign.py`

_Phoenix Automation — Self-Rescheduling for Sovereign Agents
=========================================================

This script automatically reschedules Sovereign Agent jobs for the next day
after a successful task completion.

Usage:
    python3 reschedule_sovereign.py <agent_name> <report_file.json>
    
Example:
    python3 reschedule_sovereign.py security /home/clawbot/.../task_reports/security_daily.json

The script:
1. Reads the task report to determine status
2. If status == "done" → schedules next day at same time
3. If status == "warning" → schedules follow-up in 4 hours
4. If status == "error" → schedules retry in 2 hours_

**Functions:**
- `read_report(report_path)` — Read and parse a task report JSON file.
- `get_next_run_time(agent, status)` — Calculate the next run time based on agent and sta
- `reschedule_job(agent, next_run)` — Create a new at-job for the next run time using op
- `main()`

| Modified | Size |
|----------|------|
| 2026-04-09 | 8.8KB |

### ⚠️ `session_cleanup.py`

_Session Cleanup - Delete sessions older than 7 days._

| Modified | Size |
|----------|------|
| 2026-04-09 | 1.2KB |

### ⚠️ `session_memory_manager.py`

_💾 Session Memory Manager - Auto-saves important context
Integration: Called by autonomous_loop.py or manually_

**Functions:**
- `save_session_note(session_key, context, category)` — Save a session note to memory
- `get_recent_sessions(days)` — Get recent session notes
- `archive_old_sessions(days)` — Archive sessions older than N days
- `save_daily_summary(date, summary, metrics)` — Save daily summary
- `main()`

| Modified | Size |
|----------|------|
| 2026-04-09 | 4.0KB |

### ⚠️ `telegram_alert.py`

_📱 Telegram Alert Script
Sendet Alerts nur bei PROBLEMEN/WARNUNGEN

Usage:
    python3 telegram_alert.py "Deine Warnung hier"_

**Functions:**
- `load_secrets()` — Load secrets from .secrets/secrets.env
- `send_alert(message)` — Send Telegram alert
- `main()`

| Modified | Size |
|----------|------|
| 2026-04-09 | 1.6KB |

### ⚠️ `verify_delivery.py`

_LINK VERIFIER - Pruft alle URLs vor dem Senden

Usage:
    python3 verify_delivery.py <file.md>
    python3 verify_delivery.py --check "https://example.com"_

**Functions:**
- `extract_urls(text)`
- `check_url(url)`
- `verify_file(filepath)`
- `print_report(report)`

| Modified | Size |
|----------|------|
| 2026-04-09 | 2.4KB |

### ⚠️ `weekly_review_zettel.py`

_Weekly Zettelkasten Review
- Erstellt wöchentliche Zusammenfassung
- Archiviert alte Notes automatisch
- Generiert Insights Report für Sonntag_

**Functions:**
- `get_week_range()` — Gibt Start und Ende der aktuellen Woche zurück.
- `get_notes()`
- `get_permanent_notes()` — Gibt alle permanenten Notes der Woche zurück.
- `run_review()`

| Modified | Size |
|----------|------|
| 2026-04-09 | 4.3KB |

### ⚠️ `deploy_safety.py`

_🔒 Deployment Safety Checker
Run before every deploy to ensure zero downtime.

Checks:
1. Build test (npm run build)
2. Broken links check
3. Security headers
4. Environment variables

Usage:
    python3 deploy_safety.py --project de
    python3 deploy_safety.py --project store --deploy_

**Functions:**
- `check_build(project_path)` — Run build and check for errors
- `check_broken_links(html_content)` — Check for broken internal links
- `check_security_headers(url)` — Check security headers
- `check_env_vars()` — Check environment variables are set
- `deploy_to_vercel(project_id)` — Deploy to Vercel

| Modified | Size |
|----------|------|
| 2026-04-05 | 5.1KB |

### ⚠️ `model_config.py`

_Unified Model Config for EmpireHazeClaw
Central place for all model configurations

Usage:
  from model_config import get_model_config, get_fallback_chain
  
  config = get_model_config('llm_outreach')
  models = get_fallback_chain()_

**Functions:**
- `get_model_config(task)` — Get model config for specific task
- `get_api_key_for_model(model_id)` — Get appropriate API key for model
- `get_api_base_for_model(model_id)` — Get API base URL for model
- `get_fallback_chain()` — Get the fallback model chain

| Modified | Size |
|----------|------|
| 2026-04-05 | 2.0KB |

### ⚠️ `telegram_memory_extractor.py`

_📥 Telegram Chat History Extractor & Memory Integrator v2
=======================================================
Extrahiert relevante Informationen aus Telegram Chat Exports
und integriert sie ins EmpireHazeClaw Memory System.

Usage:
    python3 scripts/telegram_memory_extractor.py <html_file> [--dry-run]_

**Functions:**
- `parse_telegram_html(filepath)` — Parse Telegram HTML export and extract messages.
- `categorize_message(msg)` — Categorize a message and return (category, summary
- `write_decision(content)` — Write a decision to memory/decisions/
- `write_learning(content)` — Write a learning to memory/learnings/
- `write_fact(content)` — Append facts to MEMORY.md

| Modified | Size |
|----------|------|
| 2026-04-05 | 8.9KB |

### ⚠️ `telegram_parser.py`

_Telegram HTML Export Parser
Extrahiert Messages aus Telegram HTML Export

Usage:
  python3 telegram_parser.py <html-file> [output-json]_

**Functions:**
- `parse_telegram_html(html_path)` — Parse Telegram HTML export and return list of mess
- `categorize_message(msg)` — Categorize a message for memory storage
- `main()`

| Modified | Size |
|----------|------|
| 2026-04-05 | 4.0KB |

### ⚠️ `vercel_monitor.py`

_🚀 Vercel Autopilot
Continuous monitoring and improvement for Big 4 domains.

Usage:
    python3 vercel_monitor.py --check      # Full Lighthouse check
    python3 vercel_monitor.py --status      # Quick status
    python3 vercel_monitor.py --deploy      # Deploy improvements
    python3 vercel_monitor.py --report      # Weekly report_

**Functions:**
- `acquire_lock()` — Acquire exclusive file lock to prevent race condit
- `release_lock(lock_fd)` — Release the file lock
- `api(endpoint)` — Call Vercel API
- `check_domain(domain, url)` — Quick HTTP check for a domain
- `check_all_status()` — Quick status check for all domains

| Modified | Size |
|----------|------|
| 2026-04-05 | 5.2KB |

### ⚠️ `evolve.py`

_🦞 Capability Evolver - Improves Agent Capabilities Over Time
Analyzes agent scripts, identifies weaknesses, suggests/implements improvements_

**Functions:**
- `scan_python_file(self, filepath)` — Analyze a Python file for issues
- `scan_directory(self, directory)` — Scan all Python files in directory
- `generate_report(self)` — Generate improvement report
- `suggest_fix(self, issue)` — Suggest a fix for an issue
- `run_scan(self)` — Run full capability scan

| Modified | Size |
|----------|------|
| 2026-03-28 | 6.2KB |

### ⚠️ `semantic_search.py`

_Semantic Search - Local Embeddings with sentence-transformers
Uses all-MiniLM-L6-v2 (384 dimensions, ~400MB, runs locally)_

**Functions:**
- `load_documents()` — Load all markdown files from memory
- `build_index()` — Build vector index from documents
- `search(query, top_k)` — Search semantic index

| Modified | Size |
|----------|------|
| 2026-03-28 | 3.3KB |


---
_Auto-generated by Sir HazeClaw_