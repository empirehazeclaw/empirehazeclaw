# Memory — 2026-04-07

## Sovereign Agent Architecture (NEW!)

### Problem: Isolated Sessions + Recurring Cron = Timeout
- Bug #61487: HTTP timeout ~61s (hardcoded, ignores timeoutSeconds)
- Bug #41558: Recurring cron jobs with isolated sessions fail

### Solution: One-Shot "at" Jobs
- `schedule.kind = "at"` instead of `schedule.kind = "cron"`
- All Sovereign Agent jobs use one-shot scheduling

### Current Schedule (One-Shot):
| Time | Agent | Job ID |
|------|-------|--------|
| 11:00 UTC | Security Officer | pending |
| 11:30 UTC | Data Manager | da52d611... |
| 12:00 UTC | Builder | 66d474ae... |
| 13:00 UTC | Research | 777b5332... |
| 14:00 UTC | QC Officer | f7e10385... |

## Security Audit Results (Morning)

### Completed:
- Gateway Token → REDACTED in openclaw.json ✅
- Brave API Key → REDACTED ✅
- .gitignore exists at /home/clawbot/.openclaw/workspace/.gitignore ✅

### Pending (6 keys):
- Telegram Bot Token (CRITICAL) → must rotate via @BotFather
- RESTIC_PASSWORD
- GitHub Token
- Buffer Token
- Google AIza Key
- SECRET_KEY

### Bug Fixed:
- Security Officer was checking wrong .gitignore path
- Fixed: /home/clawbot/.openclaw/workspace/.gitignore (not .openclaw/.gitignore)

## API Key Test Results (10:39 UTC)

| Service | Status | Notes |
|---------|--------|-------|
| OpenAI | ✅ OK | 200 |
| Telegram | ✅ OK | 200 |
| Stripe | ✅ OK | 200 |
| Minimax | ✅ OK | 200 |
| GitHub PAT | ❌ INVALID | 401 Bad credentials - MUST RENEW |
| Brave Search | ⚠️ Token OK | 422 (Request format) |
| HuggingFace | ⚠️ Timeout | Firewall? |
| Restic | ✅ OK | Exists |

### ACTION REQUIRED:
- GitHub PAT (ghp_iluknPuVi...) is INVALID - 401 error
- Must regenerate on GitHub and update secrets.env

## Workflow Summary
```
Cron (one-shot) → Isolated Session → SOUL.md loaded → cd workspace → Work → task_report.json → CEO Heartbeat checks
```


# Memory — 2026-04-07 (APPEND — 14:02 UTC)

## MetaClaw Setup — COMPLETED ✅

### Was done:
1. Metaclaw war NICHT installiert → `pip install metaclaw[all]` ausgeführt
2. Config befand sich in `/home/clawbot/.metaclaw/config.yaml` (leer)
3. MiniMax API Keys aus `~/.openclaw/secrets.env` gelesen
4. Config.yaml befüllt mit:
   - `provider: minimax`
   - `api_base: https://api.minimax.io/anthropic/v1`
   - `api_key: (aus secrets.env - MiniMax Token)`
   - `model_id: MiniMax-M2.7`
   - `auth_method: api_key`
5. Metaclaw im `skills_only` Modus auf Port 30000 gestartet

### Ergebnis:
```
======================================================================
  MetaClaw proxy ready  [mode=skills_only]
  proxy 0.0.0.0:30000 → LLM (MiniMax-M2.7)
  OpenClaw has been configured to use this proxy automatically.
======================================================================
```

### Tokenizer-Warnung (nicht-blockierend):
- HuggingFace auth fehlt (警告: No HF_TOKEN - algunas funciones no estarán disponibles)
- Proxy läuft trotzdem

## Session-Start nach Compaction
- Um 13:59 UTC: Kontext war leer nach Compaction
- Nico's "Ja" kam als Queued Message an
- Keine Erinnerung mehr worauf sich "Ja" bezog → nachgefragt

## Offene Blocker (Update 14:02):
- [ ] 4 Security Keys rotieren (Buffer, Leonardo, Google AIza, SECRET_KEY)
- [x] GitHub Token ✅ (erneuert 2026-04-07)
- [x] MetaClaw installiert + läuft ✅

## Workspace-Check nach Compaction:
- HEARTBEAT.md war noch intakt
- Memory/Notes noch vorhanden
- CEO kann nach Compaction normal weitermachen
